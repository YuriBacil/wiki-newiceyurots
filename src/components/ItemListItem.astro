---
import TierBadge from './TierBadge.astro';
import ItemSprite from './ItemSprite.astro';

interface Props {
  id: number;
  name: string;
  tier: string;
  category: string;
  attributes: string;
}

const { id, name, tier, category, attributes } = Astro.props;
---

<button
  type="button"
  data-item-id={id}
  data-tier={tier}
  class="item-list-item group w-full text-left bg-tibia-panel/80 border border-tibia-border-dark/30 hover:border-tibia-gold/40 transition-all cursor-pointer flex items-center gap-4 p-3 rounded animate-fade-in"
>
  <ItemSprite item={{ id, name }} size="md" className="shrink-0" />

  <div class="flex-grow min-w-0">
    <div class="flex items-center gap-2 mb-0.5">
      <h3 class="font-rpg text-sm text-tibia-gold font-bold uppercase tracking-wide group-hover:text-white transition-colors truncate">
        {name}
      </h3>
      <TierBadge tier={tier} />
    </div>
    <p class="text-[10px] text-tibia-muted">{category}</p>
  </div>

  <div class="hidden sm:block text-xs text-tibia-text truncate max-w-[200px] mr-4">
    {attributes || 'Sem atributos'}
  </div>

  <div class="text-tibia-gold opacity-0 group-hover:opacity-100 transition-opacity">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5"><path fill-rule="evenodd" d="M12 2.25c-5.385 0-9.75 4.365-9.75 9.75s4.365 9.75 9.75 9.75 9.75-4.365 9.75-9.75S17.385 2.25 12 2.25zm4.28 10.28a.75.75 0 000-1.06l-3-3a.75.75 0 10-1.06 1.06l1.72 1.72H8.25a.75.75 0 000 1.5h5.69l-1.72 1.72a.75.75 0 101.06 1.06l3-3z" clip-rule="evenodd"/></svg>
  </div>
</button>
